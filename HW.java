import java.util.*;
public class HW{
  int partition ( int [] data, int start, int end){
    int pivot = Math.random() * data.length
  }
  int old = data[0]; //pivot swaps with int[0]
  data[0] = pivot;
  data[pivot] = old;
}